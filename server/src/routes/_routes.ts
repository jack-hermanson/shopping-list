import { router as accounts } from "./accounts";
import { router as categories } from "./categories";
import { router as items } from "./items";

export const routes = {
    accounts,
    categories,
    items,
};
